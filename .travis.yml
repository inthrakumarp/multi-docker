sudo: required
language: generic
services:
	- docker

before_install:
	- docker build -t inthrakumarp/react-test -f ./client/Dockerfile.dev ./client

script:
	- docker run -e CI=true inthrakumarp/react-test npm run test


